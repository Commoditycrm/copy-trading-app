fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3
pydantic[email]==2.9.2
pydantic-settings==2.6.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-multipart==0.0.12
cryptography==43.0.3
httpx==0.27.2
redis==5.2.0
alpaca-py==0.33.0
# REMOVED 2026-05-31 (security): webull==0.6.1 — unofficial reverse-engineered
# SDK, root cause of the May 2026 prod supply-chain compromise (outbound
# port-scan / Kinsing signature). The package executed at import on every boot
# via app/brokers/webull.py and re-infected every rebuild. Do NOT restore until
# a vetted/official replacement exists; Webull is not in active use (Alpaca/
# SnapTrade/IBKR). See AWS abuse cases P01KSPSAGCQ4CBEVA4GE8W4AHTS / 16220881880.
# SnapTrade aggregator — official SDK. Used by the SnapTrade adapter and
# listener; requires SNAPTRADE_CLIENT_ID + SNAPTRADE_CONSUMER_KEY env vars
# (see app/config.py). Endpoints return clean 503 when unset so dev
# environments without SnapTrade creds aren't broken.
snaptrade-python-sdk==11.0.197
python-dateutil==2.9.0
requests-oauthlib>=1.3,<3
