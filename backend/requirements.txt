fastapi==0.115.6
# Pin starlette past the multipart DoS (CVE-2024-47874); within fastapi
# 0.115.6's allowed range.
starlette==0.41.3
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3
pydantic[email]==2.9.2
pydantic-settings==2.6.0
# 3.4.0 patches the CRITICAL JWT CVEs (CVE-2024-33663 algorithm confusion,
# CVE-2024-33664 JWT-bomb DoS). API-compatible with 3.3.0.
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
# 0.0.18 patches the multipart DoS (CVE-2024-53981).
python-multipart==0.0.18
cryptography==44.0.1
httpx==0.27.2
redis==5.2.0
# Excel export (services/excel_export.py). Pure Python — no compiler, no
# transitive deps. write_only mode streams rows instead of holding the sheet
# in memory, so a 50k-row export doesn't balloon the worker.
openpyxl==3.1.5
alpaca-py==0.33.0
# REMOVED 2026-05-31 (security + architecture): webull==0.6.1 — unofficial
# reverse-engineered SDK, root cause of the May 2026 prod supply-chain
# compromise (outbound port-scan / Kinsing signature). The package executed
# at import on every boot via app/brokers/webull.py and re-infected every
# rebuild. See AWS abuse cases P01KSPSAGCQ4CBEVA4GE8W4AHTS / 16220881880.
#
# Webull is also no longer in active use as a direct integration — subscribers
# now connect Webull via SnapTrade (the SnapTrade SDK below handles that
# path). Do NOT restore tedchou12/webull until BOTH conditions hold: a
# vetted/official replacement exists AND the architectural decision in
# snaptrade_decision.md is explicitly reversed.
#
# 2026-08-06: BOTH conditions now met. This is Webull's OFFICIAL OpenAPI SDK
# (webull-inc/webull-openapi-python-sdk) — a DIFFERENT package from the
# compromised unofficial `webull==0.6.1` (tedchou12) above. It powers the
# direct-Webull real-time trade signal (app/brokers/webull.py +
# services/webull_listener.py), gated behind settings.webull_direct_enabled
# (default OFF). NOTE: it imports under the same `webull` namespace, so keep
# this pinned to the OFFICIAL distribution name below — never a bare `webull`.
# The adapter imports the SDK LAZILY (never at boot), unlike the old file.
#
# ⚠️ INSTALLED WITH --no-deps (see backend/Dockerfile), NOT listed as a normal
# requirement. Reason: SDK 2.0.16 caps `cryptography<43` on Python <3.14, which
# conflicts with our CVE-pinned `cryptography==44.0.1`. The cap is conservative
# (the SDK only uses cryptography for stable HMAC/RSA request signing, unchanged
# 42→44), so we install it --no-deps and pin its OTHER runtime deps below. These
# are used ONLY by the webull SDK, so pinning them creates no other conflicts.
# If you bump to Python 3.14, the SDK allows cryptography>=43 and this dance can
# be removed (put `webull-openapi-python-sdk==2.0.16` back as a normal line).
grpcio==1.69.0
protobuf==5.29.6
jmespath==0.10.0
cachetools==5.5.2
paho-mqtt==1.6.1
six==1.17.0
# SnapTrade aggregator — official SDK. Used by the SnapTrade adapter and
# listener; requires SNAPTRADE_CLIENT_ID + SNAPTRADE_CONSUMER_KEY env vars
# (see app/config.py). Endpoints return clean 503 when unset so dev
# environments without SnapTrade creds aren't broken.
snaptrade-python-sdk==11.0.197
python-dateutil==2.9.0
requests-oauthlib>=1.3,<3
pytz
