[pytest]
testpaths = tests
addopts = -ra
markers =
    candidate_bug: asserts the CORRECT behavior for a suspected defect; a failure here is a finding, not broken test infra.
filterwarnings =
    ignore::DeprecationWarning
